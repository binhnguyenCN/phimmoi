// libs
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    overflow: "hidden",
    marginHorizontal: 10
  },
  image: {
    width: 100,
    height: 170,
    resizeMode: "cover",
    borderRadius: 5
  }
});

export default styles;
