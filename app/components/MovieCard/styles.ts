// libs
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    borderRadius: 15,
    overflow: "hidden",
    marginBottom: 20
  },
  image: {
    width: 100,
    height: 170,
    resizeMode: "cover",
    borderRadius: 5,
    margin: 15
  }
});

export default styles;
